name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  lint-and-typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint --if-present
        continue-on-error: true

      - name: Type check backend
        run: npx tsc --noEmit -p tsconfig.json

      - name: Type check frontend
        run: npx tsc --noEmit -p tsconfig.web.json

      - name: Type check frontend (unused imports gate)
        run: npx tsc --noEmit -p tsconfig.web.json --noUnusedLocals --noUnusedParameters

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: lint-and-typecheck
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Validate test discovery
        run: npm run test:discover

      - name: Run tests
        run: npm test -- --run
        env:
          NODE_ENV: test
          EXPOSURE_PERSISTENCE: 'false'
          SAFETY_SKIP_MAINNET_ACK: 'true'

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          NEXT_TELEMETRY_DISABLED: 1
