# ============================================================================================
# .env.aws — XRPL Trading Bot  (AWS EC2 deployment)
# ============================================================================================
# Copy this to /opt/xrpl-trading-bot/.env on your EC2 instance.
# Wallet secrets should be stored in AWS Secrets Manager, NOT in this file.
# ============================================================================================

# -------------------------------------------------------------------------------------------------
# [runtime]
# -------------------------------------------------------------------------------------------------
NODE_ENV=production
LOG_LEVEL=info
SERVICE_NAME=xrpl-trading-bot
PORT=3000

# -------------------------------------------------------------------------------------------------
# [security]
# -------------------------------------------------------------------------------------------------
# The bot listens on 127.0.0.1 only. Access via SSH tunnel.
# BOT_ALLOW_REMOTE=true lets it run on an EC2 instance without being
# blocked by the cloud-platform detection in server.js.
BOT_LOCAL_ONLY=false
BOT_ALLOW_REMOTE=true
BOT_API_DEV_MODE=false

# -------------------------------------------------------------------------------------------------
# [aws.secrets-manager]  ← credentials fetched at startup
# -------------------------------------------------------------------------------------------------
# Set AWS_SECRET_NAME to enable loading wallet secrets from Secrets Manager.
# The EC2 instance role must have secretsmanager:GetSecretValue permission.
AWS_SECRET_NAME=xrpl-trading-bot/wallet
AWS_REGION=ap-southeast-6

# -------------------------------------------------------------------------------------------------
# [xrpl.connection]
# -------------------------------------------------------------------------------------------------
XRPL_NETWORK=mainnet
XRPL_WSS_URLS=wss://s1.ripple.com,wss://s2.ripple.com,wss://xrplcluster.com
XRPL_MAX_RECONNECTS=10
XRPL_RECONNECT_DELAY_MS=1000
XRPL_RECONNECT_MAX_DELAY_MS=30000
XRPL_429_COOLDOWN_MS=600000
XRPL_CONNECT_TIMEOUT_MS=20000
XRPL_REQUEST_TIMEOUT_MS=15000

# -------------------------------------------------------------------------------------------------
# [wallet.signing]
# -------------------------------------------------------------------------------------------------
# DO NOT put wallet secrets here. They are loaded from Secrets Manager.
# The secret JSON should contain one or more of:
#   XRPL_SECRET_NUMBERS_MAINNET_ENC, XRPL_SECRET_PASSPHRASE,
#   XRPL_SEED_MAINNET, XRPL_SECRET_MAINNET
SIGNER_SKIP_READY_CHECK=false

# -------------------------------------------------------------------------------------------------
# [execution.mode]
# -------------------------------------------------------------------------------------------------
PAPER_TRADING=false
MAINNET_LIVE_TRADING_ACK=true
EXECUTION_ORDER_TYPE=IOC
EXECUTION_MIN_FILL_RATIO=0.25

# -------------------------------------------------------------------------------------------------
# [trading.pair]
# -------------------------------------------------------------------------------------------------
TRADE_BASE_CURRENCY=XRP
TRADE_QUOTE_CURRENCY=RLUSD
TRADE_QUOTE_ISSUER=rMxCKbTEnNHAYdsUDeRaerSwfrTgAvi2gFo

# -------------------------------------------------------------------------------------------------
# [strategy]  (copy your tuned values from local .env)
# -------------------------------------------------------------------------------------------------
# BASE_ORDER_SIZE_XRP=1.2
# SCALPER_MAX_SPREAD_BPS=12
# SCALPER_ENTRY_CROSS_BPS=12
# ... (add all your strategy params)

# -------------------------------------------------------------------------------------------------
# [monitoring]
# -------------------------------------------------------------------------------------------------
SNAPSHOT_FLUSH_INTERVAL=5
